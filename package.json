{
  "name": "rocket-grid",
  "version": "1.0.0",
  "description": "A TypeScript library to interact with the Roblox Grid Service",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && copyfiles -u 1 src/assets/* dist",
    "prepare": "npm run build",
    "test": "npm run build && node dist/test.js"
  },
  "dependencies": {
    "soap": "^1.1.6",
    "copyfiles": "^2.4.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "typescript": "^5.7.2"
  }
}
